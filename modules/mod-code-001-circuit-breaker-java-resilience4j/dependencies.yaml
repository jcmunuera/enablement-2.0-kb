# ═══════════════════════════════════════════════════════════════════
# Module: mod-code-001-circuit-breaker-java-resilience4j
# Purpose: Circuit breaker pattern dependencies
# Source: Extracted from templates/config/pom-circuitbreaker.xml.tpl
# ═══════════════════════════════════════════════════════════════════
schema_version: "1.0"

maven:
  properties:
    resilience4j.version: "2.2.0"

  dependencies:
    # ── Resilience4j ──
    - groupId: io.github.resilience4j
      artifactId: resilience4j-spring-boot3
      version: ${resilience4j.version}

    # ── AOP (required for @CircuitBreaker annotation) ──
    - groupId: org.springframework.boot
      artifactId: spring-boot-starter-aop

    # ── Actuator (resilience metrics) ──
    - groupId: org.springframework.boot
      artifactId: spring-boot-starter-actuator
